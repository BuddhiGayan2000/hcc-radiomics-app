# Copy this file to `.env` and adjust if your backend runs somewhere other
# than the default local address. The app also lets you override this at
# runtime from the "API base URL" field in Step 4 of the UI.
VITE_API_BASE_URL=http://localhost:8000
